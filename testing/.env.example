# Testing environment
# Copy to .env.local for custom setup

# URLs (defaults work if you run npm run dev for both apps)
WEB_URL=http://localhost:3000
API_URL=http://localhost:4000

# Dedicated test database (DO NOT point at production)
DATABASE_URL=postgresql://kardex_user:kardex_password@localhost:5432/kardex_test

# CI flag (set automatically by GitHub Actions)
# CI=true

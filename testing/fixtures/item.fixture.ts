/**
 * Item fixtures — placeholder for Fase 3A.
 *
 * When the Item/Stock models are added to Prisma schema, this file will export:
 *   - seedItem(input?): creates an item with category + unit
 *   - seedItemWithStock(itemInput, warehouseId, quantity): item + initial stock
 *   - seedCategory(input?)
 *   - seedUnit(input?)
 */

// TODO(Fase 3A): Implement when Item/Category/Unit/Stock models are added.

export const ITEM_FIXTURES_READY = false;

/**
 * Warehouse fixtures — placeholder for Fase 3A.
 *
 * When the Warehouse model is added to Prisma schema, this file will export:
 *   - seedWarehouse(input?): creates a warehouse (CENTRAL or OBRA)
 *   - seedCentralWarehouse(): shortcut for type=CENTRAL
 *   - seedJobsiteWarehouse(): shortcut for type=OBRA
 */

// TODO(Fase 3A): Implement when Warehouse model is added.

export const WAREHOUSE_FIXTURES_READY = false;
